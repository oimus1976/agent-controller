# Issue #131 fixed scope

In scope: publish one exact completed Jules ChangeSet as one Controller-owned implementation-branch commit and one Draft PR, with fail-closed base/scope/workstream checks and independent GitHub postcondition reads.

Out of scope: Jules AUTO_CREATE_PR, plan approval mutation, sendMessage, remediation loops, scheduler/routing, persistent workstream registry, Ready, merge, release, deploy, and autonomous LEVEL 3 execution.
